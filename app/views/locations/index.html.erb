All locations

<table class="table table-striped">

  <% @locations.each do |location| %>
    <tr>
    <td><%= location.name %></td>
    <td><%= location.state %></td>
    <td><%= link_to "Edit", edit_leader_path(location) %></td>
   </tr>
  <% end %>

</table>

<%= link_to "Add new location", new_location_path %>

<%= google_link_to("Topton NC") %>
<%= google_static_image("Topton NC") %>
