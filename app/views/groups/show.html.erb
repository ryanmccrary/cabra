Show group

<%= @group.name %>

<%= @group.street %>
<br />
<%= @group.city %>, <%= @group.state %> <%= @group.zip %>

<%= link_to "Edit", edit_group_path(@group) %>

<%= link_to "New Trip", new_trip_path( :group => @group ) %>